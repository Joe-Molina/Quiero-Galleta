---
interface props {
  name: String;
  details: string;
  price: string;
  add: string;
}

const { price, name, details, add } = Astro.props;
---

<article
  class="mx-2 my-4 shadow-xl rounded-xl flex justify-between items-center"
>
  <div class="p-3 bg-neutral-50">
    <h3 class="font-bold">{name}</h3>
    <p class="text-xs text-zinc-500">{details}</p>
    {
      add && (
        <p class="text-xs py-1 my-1 bg-red-300 w-20 text-center rounded-xl">
          {add}
        </p>
      )
    }
  </div>
  <span class="px-10 py-3">
    <span class="px-10 py-3">
      <p class="font-bold">{price}</p>
    </span>
  </span>
</article>
